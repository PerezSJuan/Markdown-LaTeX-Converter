import "./Info.css";

const Info = () => {
    return (
        <>
            <h3 className="Info">
                This tool allows you to convert markdown files to LaTeX format. You can upload multiple markdown files, configure the conversion settings, and download the resulting LaTeX file. Make sure your markdown files are properly formatted for the best results.
            </h3>
        </>
    )
}

export default Info; 